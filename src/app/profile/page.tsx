interface Props {
    className?: string;
}

const ProfilePage = ({ className }: Props) => {
  return (
    <div className={className}>
        Profile Page
    </div>
  );
};

export default ProfilePage